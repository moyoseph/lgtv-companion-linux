name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v10.1.0
      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}
      - name: Install (with dev extras)
        run: |
          uv venv --python ${{ matrix.python-version }}
          uv pip install -e '.[dev]'
      - name: Ruff
        run: uv run ruff check src tests
      - name: Mypy
        run: uv run mypy src || true   # advisory until fully typed
      - name: Pytest
        run: uv run pytest -q

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.1.0
      - name: Build wheel + sdist
        run: uv build
      - name: Verify vendored data is packaged
        run: |
          python - <<'PY'
          import zipfile, glob
          w = sorted(glob.glob("dist/*.whl"))[-1]
          names = zipfile.ZipFile(w).namelist()
          need = ["lgtvcompanion/data/lg_api_commands.json",
                  "lgtvcompanion/data/lg_api_buttons.json"]
          missing = [n for n in need if n not in names]
          assert not missing, f"missing from wheel: {missing}"
          print("wheel OK:", *need)
          PY
      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/*

  badges:
    # Recompute the tests + coverage badges and publish them to the `badges`
    # branch (read by the README shields). Only on green pushes to main.
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [test, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.1.0
      - name: Install
        run: |
          uv venv
          uv pip install -e '.[dev]'
      - name: Compute coverage + test count
        run: |
          uv run pytest -q --cov=lgtvcompanion --cov-report=json:coverage.json
          n=$(uv run pytest --collect-only -q | grep -cE '::')
          uv run python scripts/gen_badges.py coverage.json "$n" badges-out
      - name: Publish to badges branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          tmp="$(mktemp -d)"
          cp badges-out/coverage.json badges-out/tests.json "$tmp/"
          cd "$tmp"
          git init -q
          git checkout -q -b badges
          git add coverage.json tests.json
          git commit -q -m "chore: update badges [skip ci]"
          git push -f "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" badges
